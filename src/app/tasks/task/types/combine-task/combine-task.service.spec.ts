import {CombineTaskService} from './combine-task.service';
describe('CombineTaskService', () => {
  let service: CombineTaskService;

  beforeEach(() => {
    service = new CombineTaskService();
  });

  it('test', (done: any) => {
    done();
  });


});
